<%= form_for [@todo_list, @todo_item] do |f| %>

	<%= f.label :name %>
	<%= f.text_field :name, autofocus: true %>

	<%= f.submit, "Create To Do List", class: "green darken-4 white-text waves-effect waves-light btn" %>

<% end %>
