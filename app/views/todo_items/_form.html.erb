<%= form_for @todo_item do |f| %>

	<%= f.label :name %>
	<%= f.text_field :name, autofocus: true %>

	<%= f.label :complete %>
	<%= f.check_box :complete %>

	<%= f.submit, "Create To Do List", class: "waves-effect waves-light blue-grey btn" %>

<% end %>
