<%= form_for @todo_list do |f| %>

	<%= f.label :name %>
	<%= f.text_field :name, autofocus: true %>

	<%= f.label :description %>
	<%= f.text_field :description %>


	<%= f.submit class: "waves-effect waves-light blue-grey btn" %>

<% end %>
